export const AboutTable = () => {
  return (
    <>
      <h3>Loading Data</h3>
      <p>
        On the <a href="#">Table page</a>, mock data is loaded in via REST API.
      </p>
      <p>
        The page includes logic to display the appropriate state when the data
        is loading, as well as to communicate errors to the user.
      </p>
    </>
  )
}
